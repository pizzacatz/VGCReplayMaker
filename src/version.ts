// Single source of truth for the version shown in the page footer.
//
// ⚠ MAINTENANCE RULE: bump BOTH values on EVERY change that ships, and add a
// matching line to CHANGELOG.md (newest first). Use the date the change ships
// (YYYY-MM-DD) and increment the patch number (or minor/major for bigger work).
export const APP_VERSION = '0.1.0';
export const APP_UPDATED = '2026-06-16';
